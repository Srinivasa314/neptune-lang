fun collatz(x) {
	while x > 1 {
		if x % 2 === 0{
			x/=2
		} else {
			x = x * 3 + 1
		}
	}
}

const {now} = import("time")
let time = now()

for i in 1..100000 {
    collatz(i)
}

print((now()-time)*1000)