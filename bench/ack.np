fun Ack(M, N) {
    if M === 0 {
        return N + 1
    }
    if N === 0 {
        return Ack(M - 1, 1)
    }
    return Ack(M - 1, Ack(M, (N - 1)))
}

const {now} = import("time")
let time = now()
print(Ack(3,8))
print((now()-time)*1000)