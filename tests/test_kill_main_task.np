let main_task=import('vm').currentTask()
let suspendCurrentTask=import('vm').suspendCurrentTask
let task2=spawn(||{main_task.kill(new Error('killing main task'))
})
suspendCurrentTask()
throw 'unreachable'